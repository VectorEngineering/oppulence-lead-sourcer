{
  "name": "@playbookmedia/import",
  "version": "2.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "bun test src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "change-case": "^5.4.4",
    "date-fns-tz": "^3.1.3",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@playbookmedia/tsconfig": "workspace:*"
  }
}
