{
  "name": "@playbookmedia/location",
  "version": "2.0.0",
  "private": true,
  "sideEffects": false,
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "format": "biome --write .",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@playbookmedia/tsconfig": "workspace:*",
    "vitest": "^1.3.1",
    "@vitest/coverage-v8": "^1.3.1"
  }
}
