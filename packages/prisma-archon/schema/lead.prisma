// Lead references endpointId -> endpoints.id
model Lead {
  id         String   @id @default(cuid())
  endpointId String
  data       Json
  createdAt  DateTime
  updatedAt  DateTime

  // RELATION
  endpoint   Endpoint @relation(fields: [endpointId], references: [id])

  @@index([endpointId])
}